#!/usr/bin/make -f

export DEB_BUILD_MAINT_OPTIONS = hardening=+all
export PYBUILD_NAME=aiohttp-session-file

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	python3 -m pytest tests/ || true
